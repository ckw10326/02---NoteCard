# 羽球技術卡

羽球技術閃卡集，以手機螢幕尺寸呈現，方便練球前快速複習。

## 包含主題

- **技術修正**：防禦接殺、搓撥撲網、殺球連貫
- **力量傳遞**：單腳發力、轉身跳越、手臂收尾

## 使用方式

直接用瀏覽器開啟 `index.html`，或部署到任何靜態網站服務（GitHub Pages、Netlify 等）。

- 上方按鈕切換主題
- 下方按鈕或 `←` `→` 方向鍵切換卡片

## 新增卡片

編輯 `index.html` 內 `CARD_DATA` 物件，在對應主題的 `cards` 陣列新增一筆資料即可，不需要改動 HTML 結構。
